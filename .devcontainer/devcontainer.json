{
  "name": "Minicurso Bioinformática (Galaxy + Orange)",

  "build": {
    "dockerfile": "../Dockerfile",
    "context": ".."
  },

  "customizations": {
    "vscode": {
      "extensions": [
        "ms-python.python"
      ]
    }
  },

  // Comando corrigido: Executamos o 'echo -e' dentro de um shell 'bash' para garantir compatibilidade.
  "postStartCommand": "bash -c 'echo -e \"\\necho \\\"Iniciando Orange Canvas...\\\"\\norange-canvas\" >> ~/.bashrc'"
}
